# Git Stash (Temporary Save)

1. git stash -- Save uncommitted changes
2. git stash list -- View saved stashes
3. git stash pop -- Restore + remove
4. git stash apply -- Restore only
5. git stash drop -- Delete specific stash
6. git stash clear -- Delete all
