# Git Submodule

1. git submodule add <repo> -- Add repo inside repo
2. git submodule update --init -- Initialize

# Git LFS
3. git lfs install -- Enable large file storage
4. git lfs track "*.zip" -- Track large files
5. git add .gitattributes -- Save tracking rule
