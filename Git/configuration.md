# Git Configuration

1. git config --list -- View config
2. git config --global user.name "Your Name" -- Set username
3. git config --global user.email "email@email.com" -- Set email
4. git config --global core.editor vim -- Set editor
