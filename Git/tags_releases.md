# Git Tags (Release Versioning)

1. git tag -- List tags
2. git tag v1.0 -- Create lightweight tag
3. git tag -a v1.0 -m "Release" -- Annotated tag
4. git push origin v1.0 -- Push tag
5. git push origin --tags -- Push all tags
